package practice;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class AutoStepsExample {
	
	/*
	 * WebDriver driver = Hook.driver;
	 * 
	 * @Given("user select auto") public void user_select_auto() {
	 * driver.findElement(By.xpath("(//p[contains(text(),'Auto')])[1]")).click();
	 * System.out.println("User click Auto"); }
	 * 
	 * @When("user input {string} in zipcode") public void
	 * user_input_in_zipcode(String string) {
	 * driver.findElement(By.xpath("(//*[@name='ZipCode'])[1]")).sendKeys(string);
	 * System.out.println("User input zip : " + string); }
	 * 
	 * @When("user clicks startquote button") public void
	 * user_clicks_startquote_button() {
	 * driver.findElement(By.xpath("(//input[@name='qsButton'])[1]")).click();
	 * System.out.println("User clicking quote button"); }
	 * 
	 * @Then("user will navigate to personal details page") public void
	 * user_will_navigate_to_personal_details_page() throws InterruptedException {
	 * Thread.sleep(5000); System.out.println(driver.getCurrentUrl()); }
	 */
}
